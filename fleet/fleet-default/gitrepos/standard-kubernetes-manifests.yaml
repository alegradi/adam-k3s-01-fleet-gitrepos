apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: standard-kubernetes-manifests
  namespace: fleet-default
spec:
  branch: main
  clientSecretName: adam-k3s-01-github-ssh-key
  repo: git@github.com:alegradi/standard-kubernetes-manifests.git
  targets:
    - clusterName: c-xmkxq
  keepResources: true  # Do not delete resources even if the gitrepo is removed